# AGENTS 规范

## 编码要求
- 仓库内所有代码文件、配置文件、脚本文件、文档文件必须使用 `UTF-8` 编码。
- 新增文件默认使用 `UTF-8`（无 BOM），修改现有文件时不得引入其他编码格式。

## 业务与界面要求
- 所有业务功能与页面实现必须优先面向移动端展示与交互体验进行优化。
- 页面需确保在常见手机分辨率下可正常浏览、操作顺畅、布局不破版。
- 样式与交互应采用移动端优先（Mobile First）策略，再按需兼容更大屏幕。
